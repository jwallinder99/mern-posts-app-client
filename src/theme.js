import { createTheme } from '@mui/material/styles'

const theme = createTheme({
    palette: {
      mode: 'dark',
      primary: {
        main: '#00a36c'
      },
      secondary: {
        main: '#0089a3'
      }
    },
    typography: {
      fontFamily: 'Raleway'
    }
  
})

export default theme;


